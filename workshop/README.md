# Workshop notebook

**[Open in Colab](https://colab.research.google.com/github/AlexAndorra/princeton-bayes-2026/blob/main/workshop/notebooks/01_real_or_noise.ipynb)** — nothing to install, no Python assumed.

`01_real_or_noise.ipynb` is the source of truth: edit it in Colab (File → Save a copy in GitHub) or
Jupyter, and commit it with its outputs. In the room: Runtime → Run all first, then follow along.

## Delivery-path check (Colab free tier, verified 2026-09-11)

- Runtime connected, pinned `%pip install` ran; **no "Restart session" banner** was triggered.
- **Runtime → Run all completed end to end with no errors**; every figure rendered and the
  parameter-recovery output matched the local run (Story 1 recovers ~68% of true abilities inside
  its 90% intervals, Story 2 ~95%).
- Total wall time (connect + install + Run all) was about 4 minutes. Tell the room to Run all first thing.
